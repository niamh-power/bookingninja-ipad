import SwiftUI

struct AgendaScreen: View {
    let backgroundColor = UIColor(named: "backgroundColor")

    var body: some View {
        ZStack(alignment: .bottom) {
            Text("Testing")
        }
    }
}
